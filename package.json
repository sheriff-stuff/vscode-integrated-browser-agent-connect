{
  "name": "integrated-browser-agent-connect",
  "displayName": "Integrated Browser Agent Connect",
  "description": "Exposes VS Code's integrated browser to external agents (Claude Code, scripts, curl) via a local HTTP API and MCP server.",
  "version": "0.5.3",
  "publisher": "sheriff-stuff",
  "license": "MIT",
  "author": {
    "name": "Thimo Jansen"
  },
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/sheriff-stuff/vscode-integrated-browser-agent-connect.git"
  },
  "bugs": {
    "url": "https://github.com/sheriff-stuff/vscode-integrated-browser-agent-connect/issues"
  },
  "homepage": "https://github.com/sheriff-stuff/vscode-integrated-browser-agent-connect#readme",
  "keywords": [
    "mcp",
    "browser",
    "claude",
    "claude-code",
    "automation",
    "cdp",
    "chrome-devtools-protocol",
    "agent",
    "ai"
  ],
  "engines": {
    "vscode": "^1.112.0"
  },
  "categories": [
    "AI",
    "Debuggers",
    "Other"
  ],
  "enabledApiProposals": [
    "browser"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "browserBridge.start",
        "title": "Browser Bridge: Start"
      },
      {
        "command": "browserBridge.stop",
        "title": "Browser Bridge: Stop"
      },
      {
        "command": "browserBridge.status",
        "title": "Browser Bridge: Show Status"
      }
    ],
    "configuration": {
      "title": "Browser Bridge",
      "properties": {
        "browserBridge.httpPort": {
          "type": "number",
          "default": 3788,
          "description": "Port for the local HTTP API server"
        },
        "browserBridge.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start the bridge when VS Code opens"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && node esbuild.js --production",
    "check-types": "tsc --noEmit && tsc --noEmit -p tsconfig.mcp.json",
    "lint": "eslint src",
    "test": "vscode-test",
    "publish:marketplace": "vsce publish --allow-proposed-apis browser",
    "release": "scripts/release.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.21.2",
    "ws": "^8.18.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.112.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}
